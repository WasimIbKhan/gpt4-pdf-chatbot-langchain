class Chat {
    constructor(chat_id, chatTitle, docs, createdAt, history) {
        this.chat_id = chat_id
        this.chatTitle = chatTitle
        this.docs = docs
        this.createdAt = createdAt
        this.history = history
    }
}

export default Chat