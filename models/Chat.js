class Chat {
    constructor(chat_id, chatTitle, docs, createdAt) {
        this.chat_id = chat_id
        this.chatTitle = chatTitle
        this.docs = docs
        this.createdAt = createdAt
    }
}

export default Chat